name: imovel-radar

services:
  scraper:
    build:
      context: .
      dockerfile: apps/scraper/Dockerfile
    image: imovel-radar-scraper:latest
    env_file:
      - path: apps/scraper/.env
        required: false
    environment:
      API_PORT: "8000"
      # Banco Postgres externo (pg-local/Neon); host.docker.internal alcança a
      # máquina host em Docker Desktop/Windows.
      DATABASE_URL: postgresql+psycopg://postgres:teste123@host.docker.internal:5432/imovel_radar
    ports:
      - "8000:8000"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped

  bot:
    build:
      context: .
      dockerfile: apps/bot/Dockerfile
    image: imovel-radar-bot:latest
    env_file:
      - path: apps/bot/.env
        required: false
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:teste123@host.docker.internal:5432/imovel_radar
      PERSISTENCE_FILE: /data/carousel_state.pickle
    volumes:
      - bot_state:/data
    depends_on:
      scraper:
        condition: service_healthy
    restart: unless-stopped

  whatsapp-bot:
    build:
      context: .
      dockerfile: apps/whatsapp-bot/Dockerfile
    image: imovel-radar-whatsapp-bot:latest
    env_file:
      - path: apps/whatsapp-bot/.env
        required: false
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:teste123@host.docker.internal:5432/imovel_radar
      WHATSAPP_SESSION_PATH: /data/whatsapp.db
      PAIR_SECRET: dev-pair-secret
      LLM_PROVIDER: mock
      PORT: "10000"
    ports:
      - "10000:10000"
    volumes:
      - whatsapp_session:/data
    depends_on:
      scraper:
        condition: service_healthy
    restart: unless-stopped

volumes:
  bot_state:
  whatsapp_session:
