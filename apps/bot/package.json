{
  "name": "bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run python main.py",
    "build": "uv pip install -e ../../packages/shared-models && uv pip install -e .",
    "lint": "uv run ruff check ."
  }
}