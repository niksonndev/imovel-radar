# RETIRED — produção é AWS Lambda (ADR 0004). Não use este arquivo.
#
# Não define serviços. Deploy canônico: .github/workflows/infra-deploy.yml
# Workflow docker-images.yml (GHCR) é opcional e não alimenta o runtime produtivo.

name: imovel-radar-prod

services: {}
